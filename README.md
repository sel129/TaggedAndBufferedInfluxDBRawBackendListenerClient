# TaggedAndBufferedInfluxDBRawBackendListenerClient
A JMeter backend listener that builds upon the built in InfluxDBRawBackendListenerClient.  This client allows you to tag samples, as well as buffer samples before sending
